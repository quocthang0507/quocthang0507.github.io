---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {% comment %}
    Custom sitemap because a manual template exists. If you prefer the plugin output,
    simply delete this file and jekyll-sitemap will auto-generate /sitemap.xml.
  {% endcomment %}
  {% assign pages_list = site.pages | sort: 'url' %}
  {% for page in pages_list %}
    {% comment %}Skip non-public or utility pages{% endcomment %}
    {% unless page.name == 'sitemap.xml' or page.name == 'feed.xml' or page.url contains 'assets' or page.url contains '404' %}
      {% assign loc = page.url | replace: 'index.html','' | absolute_url %}
      <url>
        <loc>{{ loc }}</loc>
        {% if page.sitemap and page.sitemap.lastmod %}
          <lastmod>{{ page.sitemap.lastmod | date: '%Y-%m-%d' }}</lastmod>
        {% elsif page.last_modified_at %}
          <lastmod>{{ page.last_modified_at | date: '%Y-%m-%d' }}</lastmod>
        {% elsif page.date %}
          <lastmod>{{ page.date | date: '%Y-%m-%d' }}</lastmod>
        {% else %}
          <lastmod>{{ site.time | date: '%Y-%m-%d' }}</lastmod>
        {% endif %}
        {% if page.sitemap and page.sitemap.changefreq %}
          <changefreq>{{ page.sitemap.changefreq }}</changefreq>
        {% else %}
          <changefreq>monthly</changefreq>
        {% endif %}
        {% if page.sitemap and page.sitemap.priority %}
          <priority>{{ page.sitemap.priority }}</priority>
        {% else %}
          <priority>0.5</priority>
        {% endif %}
        {% comment %}
          Only output hreflang x-default since localized versions are handled client-side (JS i18n).
          Add real alternate language pages (e.g., /en/, /ja/) before expanding hreflang set.
        {% endcomment %}
        <xhtml:link rel="alternate" hreflang="x-default" href="{{ loc }}" />
      </url>
    {% endunless %}
  {% endfor %}
</urlset>